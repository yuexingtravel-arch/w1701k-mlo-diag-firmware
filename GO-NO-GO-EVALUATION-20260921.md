# W1701K 5+6 GHz MLO 并发项目 Go/No-Go 评估

## 决策

**No-Go：暂停继续开发“普通业务透明聚合 5 GHz 与 6 GHz 带宽”的自定义固件。**

保留 W1701K、当前 MLO-safe 固件和已经完成的稳定性修复。需要最高实际吞吐时优先使用独立 6 GHz 320 MHz；需要链路冗余或覆盖时可以使用 MLO SSID，但不把它视为吞吐叠加。

## 已经证明的能力

- Windows QCNCM865 与 W1701K 成功建立 802.11be MLD。
- Link 1 为 5 GHz 160 MHz，Link 2 为 6 GHz 320 MHz。
- AP 和客户端都报告两条 link，关联与认证层工作正常。
- MLO 下最新一次普通 TID 0 下行测试达到约 1093.8 Mbit/s。
- PS-sync 死循环未复现，Wi-Fi 7 页面保存问题已修复，AP 保持稳定。

## 没有实现的能力

- 普通 TCP/UDP 业务没有同时使用两条射频链路。
- 协商速率 5764.8 Mbit/s 是当前主链 PHY 速率，不是 5 GHz 与 6 GHz 的总和。
- 当前没有可靠、可重复的逐链 TX byte/packet 证据证明同一业务聚合。
- 单链暂停测试会影响整个 MLD，不能作为真实射频故障切换的可靠证据。

## 根因判断

当前固件的 mt7996 TX 路径已经含有 MLO 选链代码，但规则只是：

```c
link_id = (tid % 2) ? msta->seclink_id : msta->deflink_id;
```

普通 Windows 流量通常全部使用 TID 0，因此单流及普通并行流仍固定落在默认链。最新 openwrt/mt76 主线仍采用相同规则，没有可直接回移植的完整 mt7996 negotiated TTLM 或动态负载调度实现。

分 TID 测试中，TID 0 下行让 6 GHz band 的发送计数大幅增长，5 GHz band 没有增长；尝试用 iperf ToS 产生另一 TID 时，流量仍落在 6 GHz，说明当前 Windows/路由/AP QoS 路径没有形成可用的端到端 TID 分类。

## 继续开发所需工作

1. 在入口处可靠地把 DSCP/业务分类映射为 skb priority/TID。
2. 实现动态选链或 negotiated TTLM，而不是固定的 TID 奇偶映射。
3. 正确处理每链 BA、重排、队列、失败回退和功耗状态。
4. 增加逐客户端、逐链 TX/RX 字节和包计数，建立可验证性。
5. 同时验证 QCNCM865 Windows 驱动是否允许目标业务使用多个 TID/链路。
6. 在没有 UART 的情况下承担新驱动导致无线失联或启动失败的恢复风险。

这已经超出小型固件补丁范围，接近 mt76/mac80211/hostapd 与客户端驱动的联合研发。

## 成本收益

| 项目 | 评价 |
|---|---|
| 技术可行性 | 理论上可行，当前公开栈缺少完整闭环 |
| 可直接回移植代码 | 低；最新 mt76 没有解决目标问题 |
| 开发与验证成本 | 很高 |
| 无 UART 风险 | 中高 |
| 实际收益 | 受 2.5 GbE 回程和客户端实现限制 |
| 当前替代方案 | 6 GHz 320 MHz 已可达到约 0.85–1.1 Gbit/s 以上 |

## 重新启动项目的条件

仅在至少满足以下三项时重新评估：

- mt76/mt7996 上游加入 negotiated TTLM 或动态多链发送调度。
- hostapd 和 mac80211 能完整传递并应用 TID-to-Link 映射。
- QCNCM865 Windows 驱动发布说明明确支持 5+6 GHz STR 吞吐聚合。
- 驱动提供逐链 TX/RX 统计，可客观验收。
- 具备 UART 或备用同型号 AP，能安全恢复试验固件。

## 使用建议

- 追求最高、最稳定吞吐：使用 `Gemtek_W1701K_6G`。
- 追求覆盖、链路共存和未来兼容：保留 `Gemtek_W1701K_MLO`，接受当前普通业务主要走一条链。
- 不再通过强制打开 hostapd TTLM 位、随机移植 SDK daemon 或反复刷写来追求聚合，这些做法没有完整数据面支持。
